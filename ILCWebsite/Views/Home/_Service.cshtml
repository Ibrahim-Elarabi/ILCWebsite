@using ILC.BL.Models.Admin.HomeSection.Titles; 
@model (List<ILC.BL.Models.Admin.HomeSection.Service.ServiceHomeVM> serviceModel, TitleVM title)
 
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="section-title-2 text-center">
                @{
                    <h1 class="en">@Model.title.TitleEn</h1>
                    <h1 class="ar">@Model.title.TitleAr</h1>
                    <p class="en">@Model.title.DescriptionEn</p>
                    <p class="ar">@Model.title.DescriptionAr</p> 
                } 
            </div>
        </div>
    </div> 
    <div class="row">
        <div class="service-carousel">
            <!-- service-item -->
            @foreach (var item in Model.serviceModel)
            {
                <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 col-xs-12">
                    <div class="service-item">
                        <div class="service-item-image">
                            <a href="/Service/Details?id=@item.Id"><img src="./@item.ImagePath" alt="" style="height:250px;object-fit:cover;border-radius:3px 3px 0px 0px"></a>
                        </div>
                        <div class="service-item-info rtl">
                            <h5 class="en"><a href="#">@item.TitleEn</a></h5>
                            <h5 class="ar"><a href="#">@item.TitleAr</a></h5>

                            <p class="en">@Truncate(item.DescriptionEn, 40)</p>
                            <p class="ar">@Truncate(item.DescriptionAr, 40)</p>
                        </div>
                    </div>
                </div>
            } 
        </div>
        <div class="text-center">
            <a href="/Service" class="submit-btn-1" data-i18n="more-details"></a>
        </div>
    </div>

</div>

@functions {
    public string Truncate(string input, int maxLength)
    {
        if (input.Length > maxLength)
        {
            return input.Substring(0, maxLength) + "...";
        }
        return input;
    }
}