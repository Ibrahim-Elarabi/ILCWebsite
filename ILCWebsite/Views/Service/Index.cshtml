@model List<ILC.BL.Models.Admin.HomeSection.Service.ServiceHomeVM>
@using System.Globalization
@{
    Layout = "_Layout";
    ViewData["Title"] = "Index";
} 
<div class="breadcrumbs-area bread-bg-1 bg-opacity-black-70">
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <div class="breadcrumbs">
                    <p class="breadcrumbs-title" data-i18n="Services"></p>
                    <ul class="breadcrumbs-list">
                        <li><a asp-controller="Home" asp-action="index" data-i18n="Home"></a></li>
                        <li data-i18n="Services"></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<section id="page-content" class="page-wrapper">
    <!-- FEATURED FLAT AREA START -->
    <div class="featured-flat-area pt-50 pb-30">
        <div class="container">
            <div class="featured-flat"> 
                <div class="row"> 
                        <!-- service-item -->
                        @foreach (var item in Model)
                        {
                        <div class="col-xl-4 col-lg-4 col-md-4 col-sm-6 col-xs-12">
                                <div class="service-item">
                                    <div class="service-item-image">
                                    <a asp-controller="Service" asp-action="Details" asp-route-id="@item.Id"><img src="./@item.ImagePath" alt="" style="height:250px;object-fit:cover;border-radius:3px 3px 0px 0px"></a>
                                    </div>
                                    <div class="service-item-info rtl">
                                        <h6 class="en">@Truncate(item.TitleEn, 25)</h6>
                                        <h6 class="ar">@Truncate(item.TitleAr, 25)</h6>
                                        <p class="en">@Truncate(item.DescriptionEn, 30)</p>
                                        <p class="ar">@Truncate(item.DescriptionAr, 30)</p>
                                    </div>
                                </div>
                            </div>
                        }  
                </div> 
            </div>
        </div>
    </div>

</section>

<script>

    $(document).ready(function () {

        const userPreferredLanguage = localStorage.getItem("language") || "en";
        if (userPreferredLanguage === 'ar') {
            $('.page-wrapper').css({ 'text-align': 'right', 'direction': 'rtl' }); 
        }
    }
</script>

@functions {
    public string Truncate(string input, int maxLength)
    {
        if (input.Length > maxLength)
        {
            return input.Substring(0, maxLength) + "...";
        }
        return input;
    }
}